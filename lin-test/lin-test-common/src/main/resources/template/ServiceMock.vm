package ${package_name}.rpc.api;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import ${package_name}.dao.model.${model};
import ${package_name}.dao.model.${model}Example;
import ${package_name}.dao.mapper.${model}Mapper;
import com.lin.test.base.BaseServiceMock;

import java.util.List;
/**
* ${model}Service
* Create by linxiaobin on ${ctime}.
*/

public class ${model}ServiceMock extends BaseServiceMock<${model}Mapper,${model},${model}Example>
implements ${model}Service{
    private static Logger logger = LoggerFactory.getLogger(${model}ServiceMock.class);

}